<nav class="navbar navbar-macaveapp sticky-top navbar-expand-lg navbar-light">
  <div class="container">
    <a class="navbar-brand text-white" href="/"><strong>MaCave</strong></a>
      <div class="d-flex flex-row align-items-center">
        <%if !user_signed_in? %>
          <a href="/users/sign_in" class="btn btn-outline-light mr-2 rounded-pill">Sign in</a>
        <%else %>
          <div class="btn-group">

            <a type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-user-circle fa-2x"></i>
          <%end%>
            </a>
            <div class="dropdown-menu dropdown-position">
              <%= link_to "Edit profile", edit_user_registration_path, class: "dropdown-item"%>
              <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </div>
      </div>
  </div>
</nav>
